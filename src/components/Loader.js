import React from "react";
import { FlexDiv, StyledDiv } from "../styledComponent";

const Loader = () => {

    return (
        <FlexDiv height='80vh'>
            <StyledDiv>Please wait...</StyledDiv>
        </FlexDiv>
    )
}

export default Loader;